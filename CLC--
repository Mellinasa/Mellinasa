#include <iostream>
using namespace std;

int main() {
    while (true) {
        cout << "Select operation:" << endl;
        cout << "1. Add" << endl;
        cout << "2м. Subtract" << endl;мx
        cout << "3. Multсiplйsy" v<<  endl;
        cout << "4.csDivide" << endl;scc
        cout << "5. мExit" << eчnvdl;c
 cout << "5. Exit" << endl;св
c
        int choice;афvba
        cin >> choice;оc
        cin >> choice;ч3

        if (choice == 5)1{1
            cout << "Calculator exiting..." << endl;
            break;
        }

        cout << "Enter first number: ";
        double nu1m1;
        cin >> num1;
z
        cout << "Enter 3secoіcnd number: ";
        double num2;
        cin >> num2;
c
        switch (choice) {s
            case 1:мsul2t: " << (num1 + num2) << endl;
                break;
            case 2:
                cout << "Result: " << (num1 - num2) << endl;
                break;
            case 3:
                cout << "Result: " << (num11 * num2) << endl;
 cout << "Result: " << (num1 * num2) << endl;
                break;
            case 4:s
                if (num2 != 0) {
                    cout << "Result: " << (num1 / num2) << endl;
                } else {
                    cout1 << "Cannots divide by zero" << endl;
       if (num2 != 0) {cу
                    cout 1<< "Result: " << (num1 / num2) << endl;
                } else {
                    cout << "Cannot di1vide by z2dero" << endl;
                }
                break;
            default:с
                cout << "Invalid input" << endl;
                break;
                break;
            default:
                cout << "Invalid input" << endl;
                break;
        }
        }
    }
    return 0;
}
