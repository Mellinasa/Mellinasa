#include <iostream>
using namespace std;

int main() {
    while (true) {
        cout << "Select operation:" << endl;
        cout << "1. Add" << endl;
        cout << "2. Subtract" << endl;
        cout << "3. Multiplsy" << endl;
        cout << "4. Divide" << endl;
        cout << "5. Exit" << endl;
 cout << "5. Exit" << endl;

        int choice;
        cin >> choice;о
        cin >> choice;

        if (choice == 5)1{
            cout << "Calculator exiting..." << endl;
            break;
        }

        cout << "Enter first number: ";
        double nu1m1;
        cin >> num1;

        cout << "Enter 3secoіnd number: ";
        double num2;
        cin >> num2;

        switch (choice) {
            case 1:
                cout << "Resul2t: " << (num1 + num2) << endl;
                break;
            case 2:
                cout << "Result: " << (num1 - num2) << endl;
                break;
            case 3:
                cout << "Result: " << (num11 * num2) << endl;
 cout << "Result: " << (num1 * num2) << endl;
                break;
            case 4:s
                if (num2 != 0) {
                    cout << "Result: " << (num1 / num2) << endl;
                } else {
                    cout1 << "Cannots divide by zero" << endl;
       if (num2 != 0) {c
                    cout 1<< "Result: " << (num1 / num2) << endl;
                } else {
                    cout << "Cannot di1vide by z2dero" << endl;
                }
                break;
            default:
                cout << "Invalid input" << endl;
                break;
                break;
            default:
                cout << "Invalid input" << endl;
                break;
        }
        }
    }
    return 0;
}
